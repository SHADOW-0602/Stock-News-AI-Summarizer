services:
  - type: web
    name: stock-news-ai-summarizer
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: POLYGON_API_KEY
        sync: false
      - key: PORT
        value: 10000